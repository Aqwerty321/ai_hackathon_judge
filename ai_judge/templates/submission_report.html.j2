<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>AI Hackathon Report: {{ submission_name }}</title>
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      :root {
        --primary: #7c3aed;
        --primary-dark: #5b21b6;
        --primary-light: #a78bfa;
        --accent: #c084fc;
        --success: #10b981;
        --danger: #ef4444;
        --warning: #f59e0b;
        --text: #e2e8f0;
        --text-light: #94a3b8;
        --bg: #0f0a1e;
        --bg-elevated: #1a1432;
        --surface: #241b3e;
        --border: #3d2f5f;
        --radius: 12px;
        --shadow-sm: 0 1px 3px rgba(124, 58, 237, 0.15);
        --shadow: 0 4px 6px rgba(124, 58, 237, 0.2);
        --shadow-lg: 0 10px 15px rgba(124, 58, 237, 0.3);
      }

      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', sans-serif;
        background: var(--bg);
        color: var(--text);
        line-height: 1.6;
      }

      .container {
        max-width: 1400px;
        margin: 0 auto;
        padding: 2rem;
      }

      /* Header */
      header {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
        color: white;
        padding: 2rem;
        border-radius: var(--radius);
        margin-bottom: 2rem;
        box-shadow: var(--shadow-lg);
      }

      header h1 {
        font-size: 2rem;
        font-weight: 700;
        margin-bottom: 0.75rem;
      }

      .score-badge {
        display: inline-block;
        background: rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        padding: 0.5rem 1.25rem;
        border-radius: 999px;
        font-size: 1.25rem;
        font-weight: 600;
        margin: 0.5rem 0;
      }

      .meta {
        opacity: 0.9;
        font-size: 0.9rem;
        margin-top: 0.5rem;
      }

      /* Cards */
      .card {
        background: var(--surface);
        border-radius: var(--radius);
        padding: 1.5rem;
        margin-bottom: 1.5rem;
        box-shadow: var(--shadow);
        border: 1px solid var(--border);
      }

      .card h2 {
        color: var(--primary);
        font-size: 1.5rem;
        margin-bottom: 1.25rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .card h3 {
        font-size: 1.1rem;
        margin: 1.25rem 0 0.75rem;
        color: var(--text);
      }

      /* AI Highlight Box */
      .ai-box {
        background: linear-gradient(135deg, rgba(124, 58, 237, 0.15) 0%, rgba(168, 85, 247, 0.1) 100%);
        border: 2px solid var(--primary);
        border-radius: var(--radius);
        padding: 1.5rem;
        margin: 1.5rem 0;
        box-shadow: var(--shadow-lg);
      }

      .ai-box h3 {
        color: var(--primary-light);
        margin-top: 0;
        display: flex;
        align-items: center;
        gap: 0.5rem;
      }

      .ai-content {
        background: var(--bg-elevated);
        padding: 1.25rem;
        border-radius: 8px;
        margin-top: 1rem;
        white-space: pre-wrap;
        line-height: 1.7;
        box-shadow: var(--shadow-sm);
      }

      /* Markdown content styling */
      .markdown-content {
        line-height: 1.7;
      }

      .markdown-content h1,
      .markdown-content h2,
      .markdown-content h3,
      .markdown-content h4 {
        color: var(--primary-light);
        margin-top: 1.25rem;
        margin-bottom: 0.75rem;
      }

      .markdown-content p {
        margin: 0.75rem 0;
      }

      .markdown-content ul,
      .markdown-content ol {
        margin: 0.75rem 0;
        padding-left: 1.5rem;
      }

      .markdown-content li {
        margin: 0.5rem 0;
      }

      .markdown-content code {
        background: var(--bg-elevated);
        padding: 0.2rem 0.4rem;
        border-radius: 4px;
        font-size: 0.9em;
        font-family: 'Consolas', 'Monaco', monospace;
        color: var(--accent);
      }

      .markdown-content pre {
        background: var(--bg-elevated);
        padding: 1rem;
        border-radius: 8px;
        overflow-x: auto;
        border: 1px solid var(--border);
      }

      .markdown-content pre code {
        background: none;
        padding: 0;
      }

      .markdown-content strong {
        color: var(--primary-light);
        font-weight: 600;
      }

      .markdown-content em {
        color: var(--accent);
      }

      .markdown-content blockquote {
        border-left: 3px solid var(--primary);
        padding-left: 1rem;
        margin: 1rem 0;
        color: var(--text-light);
        font-style: italic;
      }

      /* Grid Layouts */
      .grid {
        display: grid;
        gap: 1.5rem;
      }

      .grid-2 {
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
      }

      .grid-3 {
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      }

      /* Metrics */
      .metrics {
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
        margin: 1rem 0;
      }

      .metric {
        background: var(--bg-elevated);
        padding: 1rem 1.5rem;
        border-radius: 12px;
        border: 1px solid var(--border);
        box-shadow: var(--shadow-sm);
        transition: transform 0.2s, box-shadow 0.2s;
      }

      .metric:hover {
        transform: translateY(-2px);
        box-shadow: var(--shadow);
      }

      .metric-label {
        font-size: 0.85rem;
        color: var(--text-light);
        margin-bottom: 0.5rem;
        text-transform: uppercase;
        letter-spacing: 0.5px;
      }

      .metric-value {
        font-size: 1.5rem;
        font-weight: 700;
        color: var(--primary-light);
      }

      /* Metrics container */
      .metrics-container {
        background: var(--surface);
        padding: 1.5rem;
        border-radius: var(--radius);
        margin: 1.5rem 0;
        border: 1px solid var(--border);
        box-shadow: var(--shadow);
      }

      /* Table */
      table {
        width: 100%;
        border-collapse: collapse;
        font-size: 0.9rem;
      }

      th, td {
        padding: 0.75rem;
        text-align: left;
        border-bottom: 1px solid var(--border);
      }

      th {
        background: var(--bg-elevated);
        font-weight: 600;
        color: var(--primary-light);
      }

      tr:hover {
        background: rgba(124, 58, 237, 0.1);
      }

      /* Badges */
      .badge {
        display: inline-block;
        padding: 0.35rem 0.75rem;
        border-radius: 6px;
        font-size: 0.85rem;
        font-weight: 600;
      }

      .badge-success {
        background: rgba(16, 185, 129, 0.2);
        color: #6ee7b7;
        border: 1px solid rgba(16, 185, 129, 0.4);
      }

      .badge-danger {
        background: rgba(239, 68, 68, 0.2);
        color: #fca5a5;
        border: 1px solid rgba(239, 68, 68, 0.4);
      }

      .badge-warning {
        background: rgba(245, 158, 11, 0.2);
        color: #fcd34d;
        border: 1px solid rgba(245, 158, 11, 0.4);
      }

      .badge-info {
        background: rgba(124, 58, 237, 0.2);
        color: var(--primary-light);
        border: 1px solid rgba(124, 58, 237, 0.4);
      }

      /* Blockquote */
      blockquote {
        background: var(--bg-elevated);
        margin: 0.75rem 0;
        padding: 1rem;
        border-left: 4px solid var(--primary);
        font-style: italic;
        border-radius: 4px;
        color: var(--text-light);
      }

      /* Charts */
      .chart-container {
        display: flex;
        gap: 1.5rem;
        flex-wrap: wrap;
        justify-content: center;
      }

      .chart {
        flex: 1;
        min-width: 300px;
        max-width: 500px;
      }

      .chart img {
        width: 100%;
        height: auto;
        border-radius: 8px;
        box-shadow: var(--shadow);
      }

      /* Lists */
      ul {
        margin: 0.75rem 0 0.75rem 1.5rem;
      }

      li {
        margin: 0.5rem 0;
      }

      li strong {
        color: var(--primary);
      }

      /* Code blocks */
      pre {
        background: var(--bg);
        padding: 1rem;
        border-radius: 8px;
        overflow-x: auto;
        font-size: 0.85rem;
        border: 1px solid var(--border);
      }

      details {
        margin: 1rem 0;
      }

      summary {
        cursor: pointer;
        font-weight: 600;
        padding: 0.5rem;
        background: var(--bg);
        border-radius: 6px;
        user-select: none;
      }

      summary:hover {
        background: var(--border);
      }

      /* Links */
      a {
        color: var(--primary-light);
        text-decoration: none;
        transition: color 0.2s;
      }

      a:hover {
        color: var(--accent);
        text-decoration: underline;
      }

      /* Footer */
      footer {
        text-align: center;
        padding: 2rem;
        color: var(--text-light);
        font-size: 0.9rem;
      }

      /* Utilities */
      .text-muted {
        color: var(--text-light);
      }

      .text-sm {
        font-size: 0.875rem;
      }

      /* Responsive */
      @media (max-width: 768px) {
        .container {
          padding: 1rem;
        }

        header h1 {
          font-size: 1.5rem;
        }

        .grid-2, .grid-3 {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>🏆 {{ submission_name }}</h1>
        <div class="score-badge">Total Score: {{ score_total }}</div>
        <div class="meta">📁 {{ submission_dir }}</div>
        {% if submission_source %}
        <div class="meta">📦 Source: {{ submission_source }}</div>
        {% endif %}
      </header>

      <div class="card">
        <h2>📊 Criteria Breakdown</h2>
      <table>
        <thead>
          <tr>
            <th>Criterion</th>
            <th>Weight</th>
            <th>Raw</th>
            <th>Normalized</th>
            <th>Weighted</th>
            <th>Description</th>
          </tr>
        </thead>
        <tbody>
          {% for criterion in criteria %}
          <tr>
            <td>{{ criterion.label }}</td>
            <td>{{ '%0.1f' % (criterion.normalized_weight * 100) }}%</td>
            <td>{{ '%0.3f' % criterion.raw_value }}</td>
            <td>{{ '%0.3f' % criterion.normalized_value }}</td>
            <td>{{ '%0.3f' % criterion.weighted_score }}</td>
            <td>{{ criterion.description or '—' }}</td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
      </div>

      <div class="card">
        <h2>🎥 Video Analysis</h2>
        <div class="metrics-container">
          <div class="metrics">
            <div class="metric">
              <div class="metric-label">Clarity Score</div>
              <div class="metric-value">{{ video.clarity_score | default(0) | round(3) }}</div>
            </div>
            <div class="metric">
              <div class="metric-label">Duration</div>
              <div class="metric-value">{{ video.estimated_duration_seconds | default(0) | round(1) }}s</div>
            </div>
            <div class="metric">
              <div class="metric-label">Sentiment</div>
              <div class="metric-value">{{ video.sentiment_label | default('n/a') }}</div>
            </div>
          </div>
        </div>
        
        {% if charts.video_metrics %}
        <div class="chart-container">
          <div class="chart">
            <img src="{{ charts.video_metrics }}" alt="Video metrics" />
          </div>
        </div>
        {% endif %}

        <h3>Transcript Excerpt</h3>
        <blockquote>{{ video_excerpt }}</blockquote>
        <p class="text-muted text-sm">Source: {{ video.transcription_source | default('unknown') }}</p>
      </div>

      <div class="card">
        <h2>📝 Text Analysis</h2>
        
        {% if text.combined_summary %}
        <div class="ai-box">
          <h3>🤖 AI-Powered Summary</h3>
          <div class="ai-content">{{ text.combined_summary }}</div>
        </div>
        {% endif %}

        <div class="chart-container">
          {% if charts.text_metrics %}
          <div class="chart">
            <img src="{{ charts.text_metrics }}" alt="Text metrics" />
          </div>
          {% endif %}
          {% if charts.scores %}
          <div class="chart">
            <img src="{{ charts.scores }}" alt="Scores" />
          </div>
          {% endif %}
        </div>

        <div class="metrics">
          <div class="metric">
            <div class="metric-label">Originality</div>
            <div class="metric-value">{{ text.originality_score | default(0) | round(3) }}</div>
          </div>
          <div class="metric">
            <div class="metric-label">Feasibility</div>
            <div class="metric-value">{{ text.feasibility_score | default(0) | round(3) }}</div>
          </div>
          <div class="metric">
            <div class="metric-label">AI Likelihood</div>
            <div class="metric-value">{{ text.ai_generated_likelihood | default(0) | round(3) }}</div>
          </div>
        </div>

        <div class="grid grid-2">
          <div>
            <h3>Project Description</h3>
            <p>{{ text.summary or 'No summary available.' }}</p>
          </div>
          <div>
            <h3>Similarity Matches</h3>
            {% if top_matches %}
            <ul>
              {% for match in top_matches %}
              <li>
                <strong>{{ match.source }}</strong> ({{ '%0.3f' % match.score }})
                <blockquote>{{ match.snippet }}</blockquote>
              </li>
              {% endfor %}
            </ul>
            {% else %}
            <p class="text-muted">✓ No notable similarities detected</p>
            {% endif %}
          </div>
        </div>

        <h3>Flagged Claims</h3>
        <div class="card">
          {% if claims %}
          <div class="grid">
            {% for claim in claims %}
            <div class="card">
              <p><strong>{{ claim.statement }}</strong></p>
              <p class="text-muted">Reason: {{ claim.reason }}</p>
              {% if claim.llm_verdict %}
              <div class="ai-box" style="margin-top: 0.5rem; padding: 0.75rem;">
                <span class="badge badge-{{ 'success' if claim.llm_verdict == 'plausible' else 'danger' }}">{{ claim.llm_verdict }}</span>
                <p style="margin: 0.5rem 0 0 0;">{{ claim.llm_rationale }}</p>
              </div>
              {% endif %}
              {% if claim.verification_result %}
              <div style="margin-top: 0.5rem; padding: 0.75rem; background: var(--surface); border-radius: 8px;">
                <span class="badge badge-{{ 'success' if claim.verification_result.status == 'verified' else ('danger' if claim.verification_result.status == 'contradicted' else 'info') }}">{{ claim.verification_result.status | default('unknown') }}</span>
                <p style="margin: 0.5rem 0 0 0;">{{ claim.verification_result.note | default('No verification note available.') }}</p>
                {% if claim.verification_result.evidence %}
                <ul style="margin-top: 0.5rem;">
                  {% for item in claim.verification_result.evidence %}
                  <li>
                    {% if item.title %}<strong>{{ item.title }}</strong>{% endif %}
                    {% if item.url %} <a href="{{ item.url }}" target="_blank" rel="noopener">Source</a>{% endif %}
                    {% if item.snippet %}
                    <blockquote style="margin: 0.5rem 0; padding: 0.75rem; background: var(--bg-elevated); border-left: 3px solid var(--primary); border-radius: 6px;">{{ item.snippet }}</blockquote>
                    {% endif %}
                  </li>
                  {% endfor %}
                </ul>
                {% endif %}
              </div>
              {% endif %}
            </div>
            {% endfor %}
          </div>
          {% else %}
          <p class="text-muted">✓ No risky claims detected.</p>
          {% endif %}
        </div>
    </section>

    <section>
      <h2>Code Insights</h2>
      <div class="card">
        {% if charts.code_metrics %}
        <div class="chart-container">
          <img src="{{ charts.code_metrics }}" alt="Code metrics chart" />
        </div>
        {% endif %}
        
        <div class="metrics-container">
          <div class="grid grid-3">
            <div class="metric">
              <div class="metric-label">Readability</div>
              <div class="metric-value">{{ code.readability_score | default(0) | round(3) }}</div>
            </div>
            <div class="metric">
              <div class="metric-label">Documentation</div>
              <div class="metric-value">{{ code.documentation_score | default(0) | round(3) }}</div>
            </div>
            <div class="metric">
              <div class="metric-label">Test Coverage Est.</div>
              <div class="metric-value">{{ code.test_coverage_score_estimate | default(0) | round(3) }}</div>
            </div>
          </div>
        </div>

        {% if gemini_insights %}
        <div class="ai-box">
          <h3>🤖 AI Code Insights (Powered by Gemini)</h3>
          <div class="markdown-content">{{ gemini_insights.analysis | markdown | safe }}</div>
        </div>
        {% endif %}

        <div class="grid grid-2">
          <div class="card">
            <h3>Lint Summary</h3>
            {% if lint_messages %}
            <ul>
              {% for msg in lint_messages %}
              <li><strong>{{ msg.symbol }}</strong> ({{ msg.path }}:{{ msg.line }}): {{ msg.message }}</li>
              {% endfor %}
            </ul>
            {% else %}
            <p class="text-muted">✅ No linting issues detected or linting not available for this project type.</p>
            {% endif %}
          </div>
          <div class="card">
            <h3>Complexity Snapshot</h3>
            {% if complexity %}
            <p><strong>Average:</strong> {{ complexity.average_complexity | default(0) | round(3) }}</p>
            <p><strong>Normalized Score:</strong> {{ complexity.normalized_score | default(0) | round(3) }}</p>
            {% if complexity.files %}
            <ul>
              {% for item in complexity.files[:3] %}
              <li>{{ item.path }} — max {{ '%0.2f' % item.max_complexity }}, avg {{ '%0.2f' % item.average_complexity }}</li>
              {% endfor %}
            </ul>
            {% endif %}
            {% else %}
            <p class="text-muted">Complexity data unavailable.</p>
            {% endif %}
          </div>
        </div>

        <div class="grid grid-2">
          <div class="card">
            <h3>Documentation Coverage</h3>
            {% if documentation %}
            <p><strong>Docstrings:</strong> {{ documentation.docstrings }} / {{ documentation.objects }} (ratio {{ documentation.ratio | default(0) | round(3) }})</p>
            {% if documentation.errors %}
            <p class="text-muted">Errors: {{ documentation.errors | join(', ') }}</p>
            {% endif %}
            {% else %}
            <p class="text-muted">No documentation metrics recorded.</p>
            {% endif %}
          </div>
          <div class="card">
            <h3>Test Results</h3>
            {% if pytest_details %}
            <p><strong>Status:</strong> {{ pytest_details.status }} (exit {{ pytest_details.returncode }})</p>
            {% if pytest_details.output %}
            <details>
              <summary>Pytest output</summary>
              <pre>{{ pytest_details.output }}</pre>
            </details>
            {% endif %}
            {% else %}
            <p class="text-muted">Pytest not executed.</p>
            {% endif %}
          </div>
        </div>
      </div>
    </section>

    <footer>Generated automatically by the AI Hackathon Judge pipeline.</footer>
  </body>
</html>
